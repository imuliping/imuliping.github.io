// starting 
//$(document).ready( main() );
// I have to add some thing

window.addEventListener('load',main() );

/**
 * starting method
 */
function main() {
    var draw = new Drawing();
    draw.init();
}
